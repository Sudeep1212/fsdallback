spring.application.name=EventsGroup1
spring.datasource.url=jdbc:postgresql://localhost:5432/Batcave
spring.datasource.username=postgres
spring.datasource.password=P@tel718
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Updated Hibernate settings (no deprecated warnings)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=false
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Disable JPA open-in-view warning
spring.jpa.open-in-view=false

# Reduced logging (optional - comment out if you want detailed SQL logs)
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# logging.level.org.springframework.jdbc.core=TRACE